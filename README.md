# Terraform ARC EKS

This repository contains Terraform code to provision an Amazon EKS cluster and deploy GitHub Actions Runner Controller (ARC).

## Components

- Terraform
- Amazon EKS
- GitHub Actions Runner Controller (ARC)
- cert-manager

## Test

This commit is used to verify the GitHub Actions ARC workflow.